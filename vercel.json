{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "SKIP_PREFLIGHT_CHECK=true react-scripts build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}